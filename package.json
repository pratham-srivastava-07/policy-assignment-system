{
  "name": "policy-assignment-system",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/worker",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @policy/shared && npm run build --workspace @policy/db && npm run build --workspace @policy/core && npm run build --workspace api && npm run build --workspace worker",
    "dev": "npm run build --workspace @policy/shared && npm run build --workspace @policy/db && npm run build --workspace @policy/core && npm run dev --workspace api",
    "prisma:generate": "npm run prisma:generate --workspace @policy/db",
    "prisma:migrate": "npm run prisma:migrate --workspace @policy/db",
    "prisma:studio": "npm run prisma:studio --workspace @policy/db",
    "dev:worker": "npm run build --workspace @policy/shared && npm run build --workspace @policy/db && npm run build --workspace @policy/core && npm run dev --workspace worker",
    "start:api": "npm run start --workspace api",
    "start:worker": "npm run start --workspace worker",
    "typecheck": "npm run build"
  }
}
