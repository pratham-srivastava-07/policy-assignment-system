{
  "name": "api",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:migrate": "npm run prisma:migrate --workspace @policy/db",
    "prisma:generate": "npm run prisma:generate --workspace @policy/db"
  },
  "dependencies": {
    "@policy/core": "*",
    "@policy/db": "*",
    "@policy/shared": "*",
    "bcrypt": "^5",
    "cors": "^2",
    "dotenv": "^17",
    "express": "^5",
    "ioredis": "^5",
    "zod": "^4"
  },
  "devDependencies": {
    "@types/bcrypt": "^5",
    "@types/cors": "^2",
    "@types/express": "^5",
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5"
  }
}
